trigger:
  branches:
    include:
    - main

pr:
- main

pool:
  name: aws-self-hosted-agent

steps:
- script: |
    echo Test => Hello, world!
    cd $(Build.SourceDirectory)/TerraformCode
    ls
  displayName: 'Test script'

